# Veterinaria (Proyecto en C++)

Este proyecto es un sistema de gestión para veterinarias, creado originalmente como trabajo universitario. Permite:

- Registrar mascotas
- Registrar turnos con el veterinario
- Iniciar sesión de usuarios
- Listar atenciones por veterinario y fecha

Actualmente estoy reorganizando y modernizando el código con el objetivo de dejarlo como proyecto de portfolio.

---

## Estructura del proyecto

src/ → Archivos fuente (.cpp)
include/ → Header con estructuras (.h)


> ⚠️ Los archivos compilados (.exe, .gch, .dat) fueron eliminados del repositorio para mantenerlo limpio y enfocado en el código fuente.

---

## 🔧 Compilación (en proceso)

Próximamente agregaré instrucciones de compilación compatibles con `g++` o `clang++`.

Ejemplo de compilación general:

```bash
g++ src/*.cpp -I include -o Veterinaria